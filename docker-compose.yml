version: '3.8'
services:
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
  ai-worker:
    build: ./ai-worker
    ports: ["8000:8000"]
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}